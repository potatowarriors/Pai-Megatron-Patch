# Alpha 현재 상태판

**규칙**: 세션 종료 시 자기 트랙의 행을 갱신하고 **커밋·push**한다. 상태는 여기에만 쓴다 — Claude auto-memory에 쓰지 않는다
(메모리는 컨테이너·노드별이라 다른 세션이 못 본다). 날짜는 절대 표기. 끝난 트랙은 "완료" 절로 내리고 정본 링크만 남긴다.

_마지막 갱신: 2026-09-25 21:40 (iter2300 벤치 완주 — T1·T3·T2·TB-2·τ³ airline 유효, SWE 는 docker 기동 실패로 무효·게이트 결함 수정; 이전: 2026-09-23 12:45 (iter2300 벤치 기동 — main1 GPU 0~6, 변환기 상대경로 결함 수정·G1~G3 PASS·T1 시작; 이전: 12:20 (sub1 본 런 재개 5차 기동·첫 iteration 게이트 PASS; 이전: 11:45 세션 재생성 4차 — 양 노드 셋업·핀·compat 595·mock PASS · sub1 재개 게이트 PASS(2301 loss 비트 동일) · main1 GPU 7 미해결 → GPU 0~6 가드·EP8 게이트 생략 ; 이전: 2026-09-18 13:41 (iter600 전 티어 완료 기록 — TB-2 0.1% · τ³ airline 30.5 유효/retail 무효(NL 판정 LLM 하드코딩); 이전: 13:10 sub1 본 런 재개 4차·첫 iteration 게이트 PASS; 이전: 12:45 main1 GPU 7 재발 확정·compat 배제 · 노드 CUDA 불가; 이전: 12:00 세션 재생성 3차 — 양 노드 셋업 PASS · main1 EP8 게이트 FAIL · sub1 PASS; 이전: 09-17 19:05 TB-2 재실행 중 133/712·miss_rate 0.36% 정상 · gemma4 복구 → τ³ 자동 체인; 이전: 17:10 SWE 1.8% · 분모 버그 수정; 이전: 12:00 :8003 정지 → 워치독; 추론 처리량 3건 적용 — 하이브리드 prefix caching 게이트 PASS·세션 고정 프록시·W 96/32/32, 에이전틱 4차 재기동; 이전: 10:05 A4 오판 2회 → 게이트 수정; 이전: 09-16 23:20 iter600 평가 체인 기동; 09-16 23:00 세션 재생성 2차 복원·sub1 본 런 iter 600 재개·main1 GPU 0~6 추론용 결정; 09-15 17:45 세션 재생성 복원 완료·main1 GPU7 재발 → sub1 임시 본 런; 09-15 05:15 세션 리셋 스냅샷; NeMo-Gym 채택 트랙 1단계; 09-14 SFT 벤치마크 에이전틱 규약 변경))_

## 세션 재생성 4차 복원 결과 (2026-09-23 ≈10:25 재생성 → 11:30)

절차·수치·정정은 `project_s/RESTORE_AFTER_REBOOT.md` **§7.9**(현행 정본). 이미지 동일(25.05). 양 노드 셋업 28분·ERROR 0·핀 09-18 과 동일·compat 595·mock 스모크 PASS.
**main1 GPU 7 미해결(사용자 확인 09-23)** → 09-16 결정 유지: env 파일 `CUDA_VISIBLE_DEVICES=0..6` 가드, mock 7 GPU, **EP8 재개 게이트 생략**. **sub1 재개 게이트 PASS** — 2301 loss 7.308551E-01 · aux · grad norm 전부 원 런과 비트 동일, 2302 rel 1.9e-6.

| 무엇 | 현재 상태 (09-23 11:45 KST) | 다음 |
|---|---|---|
| SFT 최종 본 런 | **sub1 에서 재개 5차 12:07** `outputs/alpha_baseline_48L_sft_128k_final_resume_20260923_120718`(iter 2300 승계·optimizer 포함, save 100·valid 100, 사용자 결정 12:05). 4차 런 `…20260918_125936` 은 10:24 iter 2339 로그 뒤 사망(손실 39 iters ≈3.4 h). 잔여 562 iters ≈2.0일. wandb `alpha-posttraining/pozkruas`, 런처 `outputs/launch_sft_final_resume_sub1_20260923.sh` | **첫 iteration 게이트 PASS 12:16:05** — 2301 lm loss 7.308551E-01 · seq_aux 5.488942E-01 · grad norm 0.068 · lr 3.868024E-06 전부 원 런·재개 게이트와 비트 동일, 1회차 455 s(워밍업). 관리자 세션 재생성 시 사망 → 최신 100-iter ckpt 에서 재개 |
| main1 | 셋업·핀·compat 595·mock(7 GPU) PASS. GPU 7 S/N 동일·미해결. 가드로 GPU 0~6 만 노출 | GPU 0~6 추론용만. 교체 후 실모델 EP8 게이트(§7.8 체인 6단계) 통과 전 학습 금지 |
| gh 인증 · push | 미완(양 노드) | 사용자 터미널 `export PATH=/home/work/vidsearch/tools/bin:$PATH && gh auth login --hostname github.com --git-protocol https --web && gh auth setup-git` 뒤 `git push` |
| fleet · gpu06 역터널 · τ³ · eval watch | 벤치 종료로 fleet 내려감(09-24 08:20, GPU 8장 0 MiB). 역터널 keepalive 는 살아 있음 | SWE 재실행 시 `run_suite.sh` 가 fleet·역터널 재기동 |
| **SFT 최종 런 iter2300 벤치 (main1 GPU 0~6)** | **전 티어 완주 09-24 08:20**(코어 09-23 12:31→20:03 · 에이전틱 20:03→08:20, 체인 rc 0/0, GPU 7 미사용, 워치독 이벤트 0). **T1·T3·T2 유효**: MMLU-Pro **52.2** · GPQA-D 34.5 · IFEval **65.5** · SimpleQA 4.2 · LogicKor **39.6** · RULER s1/s2/mk/mv 98.3/**60.0**/33.3/20.0 · AIME·HMMT 무효(사고마감 15%/9%). iter600 대비 MMLU-Pro +2.7 · IFEval +4.4 · LogicKor +8.4 · RULER s2 +21.7 · mk +6.6 · mv +2.5, GPQA −0.9 · s1 −1.7. **τ³ airline 30.0 유효**(iter600 30.5, harness_fail 0, n=50×4). **TB-2 0.4% 유효**(3/702, AgentTimeout 563 = 80%, 프록시 upstream_errors 18,943/54,704; iter600 0.1%). **SWE 무효** — 500/500 docker 기동 실패(CalledProcessError 426 · TimeoutExpired 74, 프록시 요청 0): docker_gc 로 이미지 캐시가 빈 상태에서 W=96 동시 pull 이 mini-swe-agent `pull_timeout` 120 s 초과. 기존 게이트가 못 잡아 0.0 이 '유효'로 집계됐던 결함 수정(종료 상태·프록시 요청 0 규칙, `KNOWN_ISSUES` 09-25) → TRACKING 무효 재집계. wandb 의 swe 0.0 은 재실행이 덮어야 함. T1 꼬리(AIME·HMMT)는 동시 요청 상한 64 = 문항당 32 표본 × 2 라 7대 중 3대만 썼음(약 2h 손실) | **① SWE 재실행 결정(사용자)**: `pull_timeout` 1800 반영 완료 → `GPUS=0,1,2,3,4,5,6 bash eval_sft/eval_new_ckpt.sh outputs/…125936 2300 swe`(에이전틱 fleet ≈3h + 이미지 pull) ② τ³ retail 판정 LLM 결정(변동 없음) ③ 다음 런 전 수학 태스크 `num_concurrent` 상향 검토 |
| NeMo-Gym · SDG U/T/D · ko_chat v3 · 터미널 코퍼스 | 변동 없음 | — |

## 세션 재생성 3차 복원 결과 (2026-09-18 10:40 경 재생성 → 11:55)

절차·수치는 `project_s/RESTORE_AFTER_REBOOT.md` **§7.8**, 서사는 `KNOWN_ISSUES` 09-18. 이미지 동일(25.05). 양 노드 셋업·핀·compat 595·mock 스모크 PASS.
**main1 은 실모델 EP8 재개 게이트 FAIL** — "GPU 7 해결" 통보(보드 S/N 동일) 뒤에도 첫 iteration all-to-all 600 s 정지. **sub1 은 같은 게이트 PASS(1001 loss 비트 동일)**.

| 무엇 | 현재 상태 (09-18 11:55 KST) | 다음 |
|---|---|---|
| SFT 최종 본 런 | **sub1 에서 재개 4차 12:59** `outputs/alpha_baseline_48L_sft_128k_final_resume_20260918_125936`(iter 1000 승계·optimizer 포함, save 100·valid 100, 사용자 결정). 잔여 1,862 iters ≈6.8일. 런처 `outputs/launch_sft_final_resume_sub1_20260918.sh` | **첫 iteration 게이트 PASS 13:08:04** — 1001 loss 7.617399E-01 · aux 5.508451E-01 · lr 1.961391E-05 · grad norm 0.092 전부 원 런과 동일. 관리자 세션 재생성 시 사망 → 최신 100-iter ckpt 에서 재개 |
| main1 | **GPU 7 재발 확정(12:40)** — compat 575 차등 게이트도 정지(compat 배제), kill 후 GPU 7 VBIOS `??` · nvidia-smi abort · 전 GPU CUDA 불가. compat 링크는 575 상태(재생성 시 초기화) | **관리자 리셋 + GPU 7 교체(RMA) 요청**. 리셋은 두 컨테이너 재생성 → sub1 런 사망 위험 |
| gh 인증 · push | 미완 | 사용자 터미널 `gh auth login` 후 push |

## 세션 재생성 2차 복원 결과 (2026-09-16 20:28 재생성 → 23:00)

절차·수치·정정은 `project_s/RESTORE_AFTER_REBOOT.md` **§7.7**(현행 정본). 요약: 이미지 동일(25.05), 셋업 30분·핀 전부 일치·compat 595·mock 스모크 양 노드 loss 비트 동일.
**main1 GPU 7 은 교체되지 않았다**(동일 S/N) — 서버관리자가 하드웨어 엔지니어에게 전달, 답변 대기. **사용자 결정 09-16: main1 은 GPU 0~6 만 추론용**, GPU 7 제외, 학습·EP8 게이트 금지.

| 무엇 | 현재 상태 (09-16 23:00 KST) | 다음 |
|---|---|---|
| SFT 최종 본 런 | **sub1 에서 재개 3차 22:49** `outputs/alpha_baseline_48L_sft_128k_final_resume_20260916_224931`(iter 600 승계·optimizer 포함, save 100·valid 100). 잔여 2,262 iters ≈8.7일. wandb `alpha-posttraining/serz0r11` | **첫 iteration 게이트 PASS 23:00:55** — 601 loss 7.945837E-01(09-15 런과 비트 동일)·aux 5.551457E-01·grad norm 0.135·max-alloc 59.7 GB·traceback 0. 다음 재생성 시 224931 의 latest ckpt 로 `load:` 갱신(RESTORE §7.7-17) |
| main1 | GPU 7 미수리(S/N 1653124027118). 정지 판별식 정상이나 결함 판정 불가. `~/.pai_megatron_alpha_env` 에 `CUDA_VISIBLE_DEVICES=0..6` 가드. GPU 0~6 bf16 matmul 756~800 TFLOP/s 정상(09-16 23:10) | **GPU 0~6 추론용만**(fleet `N_GPUS` 7). 엔지니어 답변 후 교체·EP8 게이트 |
| **SFT 최종 런 iter600 벤치 (main1 GPU 0~6)** | **전 티어 완료 09-18 05:31**(T1·T3·T2·SWE·TB-2·τ³, wandb `alpha-post-eval` iter600 16 태스크 반영). **TB-2 재실행 완료 03:59: 1/701 = 0.1%**(89 과제×8, 11h; AgentTimeoutError 564 = 79%, 과제 자체 시간 한도 중앙값 900 s; 119/712 트라이얼이 컨텍스트 262K 초과(400) 뒤 전부 실패 — restore 모드 think 누적이 원인 후보; miss_rate 1.0% 정상 · 명령 추출 57.3% · 유일 해결 `modernize-scientific-stack` 1/8). **τ³ 완료 05:30: airline pass^1 30.5 유효 · retail 6.5 무효**(harness_fail 25.2% — tau2 NL-assertion 판정 LLM 이 `gpt-4.1-2025-04-14` 하드코딩·CLI/env 오버라이드 없음 → `OPENAI_API_BASE` 인 우리 프록시로 가서 404, NL 과제 40개 중 39개의 정상 종료 표본 전부 소실; 남은 74 과제 341 표본의 값이라 대표성 없음, `KNOWN_ISSUES` 09-18). 이하 진행 기록: **T1·T3·T2 완료 09-17 06:00**(변환 EP=6 · weight 14181/14181 · ppl 5.81 · G1~G3 PASS · wandb `alpha-post-eval` A 런 곡선에 iter600 기록). MMLU-Pro **49.5** · GPQA-D **35.4** · IFEval **61.1** · SimpleQA 3.8 · LogicKor **31.2** · RULER s1/s2/mk/mv 100/38.3/26.7/17.5 · AIME·HMMT 무효(사고마감). phase-1 iter600 대비 T1 3종 우위(48.6/32.2/55.6), LogicKor·RULER 열세(40.2 · s2 71.7). RULER 실패 대부분이 `0, 1`·`0.5, 1.0…` 퇴행 출력(Reasoning-Off). 프로브: identity FAIL(53%/63%, `no_lead`) · 누출 0 · 유령 2/33 FAIL. **에이전틱은 A4 오판으로 두 번 건너뜀**(1차 되물음 1표본 · 2차 thinking OFF 8/8 "도구 못 씀" 거부, A5 ON 은 4/4 PASS) → `judge_a4` 관측/미관측 분리 + ON 폴백(테스트 26 PASS, `KNOWN_ISSUES` 09-17) 후 09:52 3차 기동 → SWE 64/500 에서 **사용자 결정(11:00): 추론 처리량 3건 적용 위해 정지** → 게이트 PASS 후 **11:32 4차 재기동**(`PREFIX_CACHE=1 SWE_RESUME=1`, 세션 고정 프록시, W 96/32/32; SWE 64건 승계). W=96 실측: 실행 요청 73·생성 6,211 tok/s(4.4×)·요청 773/min(5.1×)·prefix 적중 98.7%·prefill 0.06 s. **11:36 :8003(GPU 3) EngineCore 정지**(126 W·생성 0, SIGKILL 재기동, 세션 16 재배정) → `fleet_watchdog.py` 가동, 재발 GPU 로 하드웨어/align 판별(`KNOWN_ISSUES` 09-17). **SWE 완료 14:36: 9/500 = 1.8%**(유효). TB-2 는 16:35 중단(101 트라이얼, 보상 0/87, 시간 한도 초과 85%; miss_rate 32.5% 무효는 프록시 분모 버그 → 수정, 실제 0.7%). TB-2 는 16:05 중단, **τ³ 16:06 시작 → 16:15 부터 상대역 gemma4 가 전 경로 404** → 16:17 종료(무효, `KNOWN_ISSUES` 09-17). 구 스위트는 실행 중 `run_suite.sh` 편집 탓에 docker gc 직후 구문 오류로 사망(집계·wandb 수동 복구 17:00, 함정 표). **TB-2 재실행 16:57 기동**(`run_suite.sh … tb2`, W=32, timeout 배수 1.0, 고친 프록시, 로그 `tools/bench_logs/<TAG>_tb2_suite.log`, 약 11 h). **gemma4 상대역 18:57 복구 확인(200)** → τ³ 는 TB-2 종료 후 `outputs/tau_after_tb2_iter600_20260917.sh` 가 자동으로 `run_suite.sh … tau`(원래 상대역, W=32) + 워치독. 병행하지 않는 이유: TB-2 시간 한도 조건 보존. 워치독 재발 0(13:21 이후). **다음 fleet 기동부터 `GPU_MEM_UTIL` 0.95**(사용자 결정 13:05, `serve_alpha.sh` 기본값). 실측·게이트·남은 레버·RL 트랙 확인 항목은 `SFT_BENCHMARKS.md` §2.5 "추론 처리량". iter600 의 thinking OFF 도구 거부는 When2Call(no-think·전부 미호출) 학습 신호로 설명되는 정상 행동(사용자 지적 09-17, `KNOWN_ISSUES` 09-17). RULER Reasoning-Off 퇴행 출력만 미해결 | ① τ³ retail 재측정 — 판정 LLM 지정 수단(tau2 `config.py` env 오버라이드 또는 프록시 모델 별칭) 추가 후 상대역 gemma4(무료·추이용) vs Gemini(판정 품질·비용) 사용자 결정 → `TAU_DOMAINS=retail TAU_FRESH=1 run_suite.sh … tau`(≈1h, main1 fleet 필요 — 현재 main1 CUDA 불가) ② TB-2 컨텍스트 초과 기여는 strip 대조 또는 복원 창 제한 스모크(아래 열린 결정의 `tau_proxy` 옵트인 플래그)로 분리. RULER Reasoning-Off 숫자 나열 퇴행(도구 무관, no-think 채팅 데이터 9.4% 존재)은 원인 미상 — iter900 재측정 또는 thinking ON RULER 대조로 분리. A4 는 ON 단독으로 줄일지 결정(OFF 8회는 When2Call 정책 진단일 뿐). LogicKor 하락은 ko_chat v1/v2 폐기의 예상 효과 — v3 편입 후 재측정 |
| mock 스모크 종료 시 SIGSEGV | 양 노드 rank 4, 저장 성공 뒤 teardown, 재현 1회 clean → 플레이크 | 스모크 판정을 rc 대신 iteration+saved 로(RESTORE §7.7-13). KNOWN_ISSUES 09-16 |
| iter2448 스위트 · eval watch · fleet | **정지** 유지(sub1 학습 전용). gpu06 역터널은 iter600 에이전틱 단계에서 main1 이 띄운다 | main1 fleet 는 iter600 평가가 먼저(사용자 09-16). 그 뒤 iter2448 에이전틱·T2 재개 여부 결정 |
| gh 인증 · push | 미완(세션 재생성으로 소실) | 사용자 터미널에서 `gh auth login` 후 `git push` (RESTORE §7.7-2) |
| NeMo-Gym · SDG U/T/D · ko_chat v3 · 터미널 코퍼스 | 변동 없음 | — |

## 세션 재생성 복원 결과 (2026-09-15 15:07 재생성 → 17:45) — 이력


절차·정정·검증 수치는 `project_s/RESTORE_AFTER_REBOOT.md` **§7.6**(현행 정본). 요약: 이미지 25.03→25.05 변경에도 스택 재구성·핀 일치·양 노드 mock 스모크 PASS·sub1 재개 스모크 PASS(loss 비트 동일).
**main1 GPU 7 하드웨어 결함 재발**(Xid 109→120, `KNOWN_ISSUES` 09-15) → 교체 요청, main1 GPU 작업 불가.

| 무엇 | 현재 상태 (09-15 17:45 KST) | 다음 |
|---|---|---|
| SFT 최종 본 런 | **sub1 에서 재개 17:43** `outputs/alpha_baseline_48L_sft_128k_final_resume_20260915_174309`(iter 300 승계, save 100·valid 100, 사용자 결정 — 중단 손실 ≈9 h 상한). 잔여 2,562 iters ≈9.8일 | **첫 iteration 게이트 PASS 17:58** — 301 loss 0.8372513(09-14 런과 비트 동일)·302 0.8318815·329.4 s/iter·263 TFLOP/s/GPU·max-alloc 60.0 GB·traceback 0, wandb `alpha-posttraining/pp9vb2ax`. 관리자 리셋(두 컨테이너 재생성) 시 마지막 100-iter ckpt 에서 §7.3 재수행 후 재개 |
| main1 | GPU 7 `0000:db:00.0` 보드 불량 — nvidia-smi abort·전 GPU CUDA 불가 | 관리자 교체 대기. 복귀 시 EP8 재개 2-iter 스모크 통과 전엔 사용 금지 |
| iter2448 스위트 · eval watch · fleet (sub1) | **정지** — sub1 이 학습 중, main1 없음. SWE 예측 435/500 gpu06 보존, 역터널은 살아 있음 | main1 복귀 후 재개(`eval_new_ckpt.sh … 2448 agentic,t2`, `--redo-existing` 결정은 평가 세션) |
| 채팅 서빙 (GPU3 :8001/:8080) | **이 클러스터 아님** — 다른 컴퓨팅 세션의 노드 작업(사용자 확인 09-15) | — |
| NeMo-Gym · SDG U/T/D · ko_chat v3 · 터미널 코퍼스 | 변동 없음 | — |

## 세션 리셋 스냅샷 (2026-09-15 05:15 UTC, main1 GPU7 Xid 장애 → 멀티노드 세션 재생성 예정) — 이력

절차·소실 목록·검증은 `project_s/RESTORE_AFTER_REBOOT.md` **§7**. 리셋 시점의 라이브 상태와 재개 지점(위 표가 현행):

| 무엇 | 리셋 시점 상태 | 재개 지점 |
|---|---|---|
| SFT 최종 본 런 (main1) | iter 441 사망 03:46 UTC(EP all-to-all NCCL 타임아웃·SIGABRT, GPU 7 `0000:db:00.0` VBIOS 판독 불능). 프로세스 없음 | ckpt **iter 300**(`…final_resume_20260914_013856/checkpoints`). 재개 프리셋 `load:` 가 A 런 iter150 을 가리키므로 iter 300 경로로 수정 후 2-iter 스모크 → 본 런 |
| iter2448 스위트 (sub1) | fleet 8대 + lb_proxy :8100 + `run_suite.sh` → `run_swe.sh` 가동 중. T1·T3 완료, **SWE 예측 435/500**(gpu06 `/opt/swebench/preds_…iter0002448`, 보존), TB-2·τ³·T2 미착수 | `eval_new_ckpt.sh … 2448 agentic,t2` 재실행. `run_swe.sh --redo-existing` 이라 435건 재사용 여부는 평가 세션 결정 |
| 채팅 서빙 (main1 GPU3, :8001/:8080) | 다운 | ~~RESTORE §7.3-10~~ → **다른 컴퓨팅 세션의 노드 작업**(사용자 확인 09-15), 이 클러스터 범위 밖 |
| SDG U·T·D · ko_chat v3 · 터미널 코퍼스 | 완료(GLM 잡 `DONE runbook`) | 없음 |
| NeMo-Gym | 1단계 완료(NFS 설치, 조치 없음) | 2단계 게이트는 fleet 여유 시 |
| gpu06 `alpha-eval` | 세션 밖, 무영향. 역터널만 sub1 에서 재기동 | RESTORE §7.3-8 |
| 리포 미커밋(다른 세션) | `CLAUDE.md`·`.gitignore`·`megatron_patch/CLAUDE.md`·`toolkits/…/CLAUDE.md` | NFS 라 소실 없음, 소유 세션이 커밋 |

## 진행 중

| 트랙 | 상태 (2026-08-25) | 다음 할 일 | 정본 |
|---|---|---|---|
| **SFT 최종 단일 런 (2026-09-13~, 사용자 결정: phase-1/2 폐기·Ultra 비율·DiLoCo 2노드)** | **준비 중 (09-13 02:00)** — ① 신규 멤버 5종 변환·검증 완료: `chat_v2_on_scrub`(스크럽판+fan-out, 21,200 bins) · `chat_v2_off_scrub`(10,520) · IF-Chat-v1 `ifchat_v1_chat_if`(426k행→fan-out 633k 샘플, 13,966 bins, 실 1.83B) · `ifchat_v1_structured`(157) · When2Call `when2call_v1`(14,876행 전부 미호출, 66 bins) — verify PASS 5/5·render 봉투 0. ② 트리 `sft_packed_128k_final_pad16` 57 멤버(kochat v1/v2·ARC-AGI·구 opencode/swe 변형 제외; bins<100 멤버는 병합·2벌로 해소 — 첫 스모크 02:03 valid 0-doc 정지 실측). ③ 블렌드 `configs/data/sft_128k_final_blend.yaml` = **2,862 iters × GBS 160 = 60.02B**, Ultra `data_blend_raw.json`(합 105.8) 카테고리 비중 토큰 기준(chat/IF/구조화 27.4 · 코드 27.5 · 수학 16.1 · 과학 12.3 · 다국어 7.1 · 에이전틱 3.8 · 금융 2.9 · 저노력 1.9 · safety 0.5 · identity 0.5). ④ 프리셋 `sft_128k_final.yaml`(단일) · `sft_128k_final_diloco.yaml`(노드당 1,431 iters, pretrained-checkpoint 방식) · 런처 `NODE1_ENV`(sub1 jit595). ⑤ 게이트 체인 `eval_sft/probe_ckpt.sh` 작성. **main1 2-iter 스모크 PASS 02:27** (iter1 loss 1.069/515 s, iter2 1.039/324.7 s, 267 TFLOP/s/GPU, 캐시 콜드 빌드 9분). ⑥ **A/B 시작**: A = main1 단일 `alpha_baseline_48L_sft_128k_final_20260913_022854`(150 iters, `--exit-interval 150`, 02:29 기동, wandb alpha-posttraining) · **sub1 jit595 스모크 PASS**(loss 비트 동일 1.069033→1.038569, 321.7 s/iter). 사용자 확정 02:50: When2Call 6.4ep·Ultra 27/27. 체인 `scripts/sft_final_chain.sh` 가동(iter 100 + sub1 유휴 → probe, A 종료 시 B 자동 기동). **A 150 iters 완주 15:47**(loss 0.882, ckpt 100·150) · **A′ 100 iters 완주**(sub1 9.5h 안정, A 와 loss 차 ≤2e-4). B 1차 기동은 런처 `CUDA_DEVICE_MAX_CONNECTIONS=32` 강제 → CP8 assert 로 실패, 수정 후 **B 재기동 16:12**(DiLoCo 2노드 150 iters, 종료 ≈09-14 06:30). A iter100 probe: identity 0/30·유령 4/33 = 시작점 수준(아직 무의미). **09-14 01:00 DiLoCo 기각(사용자)** — 같은 데이터 효율 0.6, 벽시계 1.2×(`study/diloco_sft_ab.md`). **⚠ 09-15 03:47 main1 GPU 장애로 중단(iter 441, EP 그룹 NCCL 타임아웃 → SIGABRT; 이후 nvidia-smi 'Failed to fill in device global IDs'·cuda 텐서 생성 코어덤프 = 드라이버/GPU 수준, 관리자 리셋 필요). 마지막 ckpt iter 300 → 141 iters(≈12 h) 유실. 복구 시 iter 300 에서 자동 재개(1시간 주기 점검).** 본 런 = 단일 노드 재개 01:32 `…sft_128k_final_resume_20260914_013856`(A iter 150 승계, save 300·valid 100 — 사용자 09-14, 잔여 2,712 iters ≈9.8일, 종료 ≈09-24), sub1 `sft_final_eval_watch.sh` 300 iters 마다 probe+T1 | iter 300 T1(≈09-15 새벽)을 phase-1 iter 300(47.0/32.0/65.6)과 비교 → 열세면 phase-1 이어가기로 전환 검토. 매 300 iters probe(identity·유령 호출) 추이. 게이트 체인(identity 프로브·유령 호출 재생 매 100 iters, T1 매 300) | `docs/SFT_FINAL_PLAN.md` |
| **SFT phase-2 능력 추가 (절충안 M, 사용자 승인 2026-09-04)** | **⛔ 폐기 2026-09-13 (사용자 결정) — phase-1·phase-2·phase-3 계보를 버리고 전 데이터셋을 합친 최종 단일 SFT 로 대체(`SFT_FINAL_PLAN.md`). ckpt 파일은 지시 전까지 보존.** 이하는 폐기 전 기록. **배경**: Ultra 기술보고서 16쪽이 Super 검색 트라젝토리(= Agentic-v2 `search`)를 retain 했음을 확인 → 08-24 미편입 결정 번복. 미사용 SFT 16종 실측(13종 ≈240B, `SFT_RL_DATASETS.md` §2.8) 후 편입 범위 확정: Agentic-v2 3종·Chat-v2·SWE-v2/v1·Science-v1·Finance-v1·Lean·Multilingual-v1(12). 리플레이는 카테고리별(형제 있는 카테고리 15%·cp/math 50%·한국어/IF/ml/identity 100%). **데이터·블렌드 완료(09-04)**: tool_calling.jsonl 절단본(1.2%) 재다운로드 정정, search held-out 300 분리, 23 신규 멤버 변환(sub1, `convert_sft_128k_mixed_p2b.sh`) → `verify_sft_bins --tree p2` **49/49 PASS**, `render_check.py` 23셋 봉투 0. 블렌드 `sft_128k_mixed_blend_p2.yaml` 재산출 = **602 iters(12.62B, ≈2.3일)**, verbatim 41.6%·신규 37.4%·확장 21.0%, 한국어/IF/ml/identity 비중 phase-1 동일, 에이전틱 26.4→27.2%. preset `sft_128k_full_p2.yaml` train-samples 96,320·load=교체 재개 런. 검색 에이전트 하니스 `eval_sft/search_agent_eval.py` selftest 24/24(모델 수치는 서버 부재로 미측정). **sub1 스모크 3회 실패 = sub1 compat libcuda 595(08-29 vLLM용 스왑) + Backend.AI libcudahook — sub1 은 학습 불가 노드 — 09-09 jit595 우회로 해소**(`KNOWN_ISSUES` 09-04; 데이터·프리셋 무관, 캐시 398 파일은 프리빌드됨). **자동 연계 런처 arm(09-04, main1)**: `scripts/launch_p2_after_phase1.sh` → 2448 완주 → GPU 유휴 → sanity → 본 런 → 첫 iteration 게이트(실패 시 `P2_CHAIN_ALERT.txt`), 로그 `outputs/p2_launcher.log` | ① ~~정식 G-P5~~ **PASS(09-05 18:01, sub1 compat 570, 사용자 기동)**: loss 0.711→0.733, max-alloc 55.9GB, 322 s/iter, nan·traceback 0, compat 595 복원 확인 ② ~~개시~~ **phase-2 본 런 개시 2026-09-07 16:34 KST (자동 연계, `outputs/alpha_baseline_48L_sft_128k_full_p2_20260907_073414`, wandb online)**: 첫 iteration 게이트 통과(loss 0.709, nan 0, traceback 0), iter 8 loss 0.731·grad norm 0.61·307 s/iter·max-alloc 55.8GB → 602 iters ≈ 50.7h → **~09-09 20:00 KST 종료** ③ 병행: 2448 HF 변환 → G-P6 기준선(T1·에이전틱(TB-2 전환)·프로브 + 검색 format/live; `TAVILY_API_KEY` 는 `.env`) ④ 100 iters 마다 G-P7(save/eval 100) ⑤ 종료 후 전 스위트, LogicKor·IFEval 무회귀. **→ 2026-09-09 결과: 완주(iter 602, valid 0.587) 했으나 회귀 2건 발견 → B안(사용자)**: P1 도구 선언 시 과잉 호출(도구 25종 주입 재생 유령률 iter1500 0/33 → p2 8/33; 원인 Agentic-v2 범용 API 호출률 96%·미호출 3%) · P2 Chat-v2 교사 자기귀속 205턴("trained by Google") · P3 제작자 프로브 37%(phase-1 부터, 게이트 미배선). 도구 없는 일반대화는 무회귀(reasoning 길이·언어·코딩·정체성 전 ckpt 동등). **phase-1 최종 iter2448 에서 교정 블렌드로 phase-2 재실행** 예정 — 교정: Chat-v2·ko_chat 자기귀속 행 스크럽(`toolkits/sft_data_preprocessing/scrub_vendor_self_attribution.py`), Agentic-v2 미호출 예시 2:1(When2Call), identity 상향+프로브 배선, ko_chat v3 교체, 터미널 공개 코퍼스 흡수. phase-3 런은 iter 29 에서 종료, iter602 T1·T3 벤치는 사용자 지시로 중단(양 노드 합성 투입). 서사 `KNOWN_ISSUES` 09-09 | `SFT_PHASE2_PLAN.md` §11, `KNOWN_ISSUES.md` 09-09 |
| **후속 SDG 트랙 U·T·D 완료 2026-09-13 00:30 — bins 5종 verify PASS·render clean: `usab_v1_think` 20,272행/273 bins(학습 32.5M)·`usab_v1_nothink` 8,539/35(3.1M)·`kotool_v1` 6,134/81(4.1M)·`search_ko_v1` 3,992/1,049(21.0M)·`research_ko_v1` 1,071/521(8.8M) = 학습 ≈70M tok, ko-chat v3 2종(233M)과 함께 **최종 블렌드 `sft_128k_final_blend.yaml` 등록 완료(09-13, B안 계획은 최종 단일 SFT 로 대체)**** (main1 GLM + sub1 DSV4, 2026-09-12~, 사용자 승인 24h 예산 계획, 유료 검색 API 폐기 → 로컬 BM25) | **진행 중**: U 본생성 36k 잡(채택 84%, 7k 완료), T 2차 8k 잡(생성 시점 규칙으로 되묻기 확보), D 루프(D1 정답 일치 68%·D2 심판 ≥0.8 19/20) 기동 04:50 KST. 검색 코퍼스 v2(3.43M passage, 헤드라인 제목, NIKL url 없음). P0 표본 사용자 확인 2회 완료. B안 블렌드 등록은 이 트랙들 완료 후. 설계 정본 `sdg/{usab,search,kotool}/README.md` | ① U·T·D 완료 → P3 검수 → P4 128k bins(`usab_v1_*`, `kotool_v1`, `search_ko_v1`, `research_ko_v1`) verify·render ② B안 블렌드 계획 보고 | `sdg/usab/README.md`, `sdg/search/README.md`, `sdg/kotool/README.md` |
| **ko_chat v3 한국어 chat 재합성** (main1+sub1, 2026-09-09~) | **P1 트랜치 완료 09-11 18:10 — 생성 92,924행(GLM 42,305·DSV4 50,619), P3 사고 p50 1,408 tok(NVIDIA 1,256)·자기귀속 0·한자 0, P4 `sft_packed_128k_terminal_pad16/kochat_v3_think`(1,709 bins, 학습 210.7M tok)·`kochat_v3_nothink`(214 bins, 22.6M) verify PASS·render clean → 최종 블렌드 `sft_128k_final_blend.yaml` 등록 완료(09-13, 1.70ep)**. GLM 2대 병행(sub1, 사용자 승인 09-11)·심판 보류→DSV4 일괄 판정·리젝 DSV4 구제 4,315. 서사 `sdg/ko_chat_v3/README.md` P2~P4 절, `reports/P3_REPORT_P1.md`. 이전: **P2-B(DSV4 생성·GLM 심판) 완료 09-10 14:30 — 41,448행, 채택 99.2%·자기귀속 0·한자 0·no-think 파생 29.5%, 사고 tok 중앙값 S1 568/S2 503~616/S4 1,261(NVIDIA 1,256)**. P2-A(GLM) 진행 중 0.13 시드/s — GLM-5.3-Flash 는 **하이브리드(KDA 34층+희소 MLA 11층)** 라 랭크당 ≈28 요청에서 KV 포화(선점) → 동시성 ≈185 로 캡, fp8 KV 는 FlashInfer MLA sparse 가 거부, prefix caching 적중 0; Max effort 폭주(빈 답변·길이 초과)로 GLM 토큰 24% 낭비·S4 맥락 시드는 양샘플 리젝 39%. **사용자 결정 대기**: v2 지시문(−21%)·S4 DSV4 전담·A:B 재배분(현행 1:1 유지 시 ≈50h). **P1 본 트랜치 착수(09-10 12:30 KST, 사용자 승인)**: 시드 10만 행을 교사별 파일로 동시 생성 — GLM(main1) 기사 20k·현지화 20k·맥락 8.5k, DSV4(sub1, **seqs 256 재기동 → 7,849 tok/s@256**, 128 대비 +58%) 기사 20k·현지화 20k(`--offset` 서로소)·맥락 8.5k, 실사용자 2,348(≥25자·히스토리 벤더 자기귀속 제외; 목표 3k 미달분은 타 트랙 흡수). 병합 `merge_seeds.py` = conv_id 해시 A/B(A GLM 생성·DSV4 심판, B 반대). GLM 은 시드 3종 255 동시에서 4,113 tok/s·KV 90% 포화 → P2 는 시드 완료 후 순차. **P0 파일럿 완료(09-10, `sdg/ko_chat_v3/reports/P0_PILOT_REPORT.md`)**. 계획(사용자 승인): 1차 10만 행 = 현지화 40k·**기사 기반 40k**(NIKL 신문 말뭉치 974k, 모드 A 문서제공/B 영감형)·실사용자 3k·한국 맥락 17k; lmsys 유래 포함(사내 전용)·기사 원문 포함. r1 이 짧았던 원인 = 시드 단순(lmsys 절반 ≤10tok) + 한국어 프롬프트에서 교사 사고 1/3(언어 효과, 같은 영어 프롬프트에선 GLM = NVIDIA 동급) → "철저 사고" 지시로 회복. P0: 기사 396 프롬프트 리젝 0, GLM 사고 중앙값 2.4~3.4k tok(NVIDIA 1,256 의 2.3×, 영어), **DSV4-Flash-0731(sub1, Marlin mxfp4) 사고 505~782 tok 한국어**·한자 0·채택 100%; 심판 = 상대 교사 저효율(Qwen 제거, 사용자). 게이트 재보정(퇴행 오탐)·구제 스크립트. 이전: **r1 완주(09-10 새벽, 4,573 s)**: 채택 **2,327/2,561 (90.9%)**, 사고 중앙값 GLM 1,666자·Qwen 983자, 한자 0, 자기귀속 0, 멀티턴 44.7%; 심판 실패 12%(사고 예산 소진)·퇴행 게이트 오탐 → r2 코드 반영 완료(심판 no-think+순서 무작위, 게이트 완화, 리젝 전문 보존). P2 스크럽: Chat-v2 on 1,823·off 829행 드롭(0.2/0.08%, 오탐 포함 → 정규식 조일 것). 검수 `sdg/ko_chat_v3/out/R1_INSPECT.md`. 이전: v1/v2 폐기(비-reasoning 교사 가짜 사고) 후 NVIDIA Chat-v3 레시피로 재합성: 실사용자 시드(lmsys Korean 2,561) → 교사 2종 라운드로빈 **GLM-5.3-Flash(main1 :8000, 4,002 tok/s@128) + Qwen3.8-Flash-Next-FP8(sub1 :8300, TP8+EP, vLLM 레시피 정합, 9,178 tok/s@256)** N=2 생성 → 상대 교사 쌍대 심판 → 게이트(빈답·특수토큰·**중국어 혼입**·벤더 자기귀속·사고 퇴행). P1 파일럿: 네이티브 사고 중앙값 1,444자(v1/v2 204). 두 교사 모두 영어(혼합) 사고·한자 0 → reasoning 언어 규칙(영어·영한 OK, 중국어 금지) 통과. DSV4-Flash-0731 은 Blackwell 전용이라 H100 불가(기각). WildChat-1M-Full gated 거부(재요청 필요). 산출 `sdg/ko_chat_v3/out/v3_r1_lmsys.jsonl` | ① r1 결과 검수(채택률·리젝 분포·심판 편향) ② 시드 확장: WildChat 접근 재요청·현지화 재작성·트랙 B ③ 변환(`build_alpha_sft_idxmap.py`)·`verify_sft_bins`·render_check ④ 누락 도메인 트랙: deep-research/브라우저 검색·usability/structured outputs(사용자 확정), office 는 미래 | `sdg/ko_chat_v3/README.md`, `KNOWN_ISSUES.md` 09-09 |
| **터미널 에이전트 SFT 데이터 (2026-09-07 합성 착수 → 09-10 공개 코퍼스 채택)** | **공개 `nvidia/Nemotron-Terminal-Corpus` 366,154행 채택(사용자 09-10), 학습 투입 준비 완료 — 최종 블렌드 `sft_128k_final_blend.yaml` 등록 완료(09-13, 0.149ep)**. 변환 365,705행(파싱실패 턴 splice 328k·null 말미 17.6k 제거, 사고 턴 99.8%) → 6 멤버 `sft_packed_128k_terminal_pad16/ntc_v1_{code,math,swe,syn_easy,syn_medium,syn_mixed}` 41,941 bins, 실토큰 5.48B/학습 3.35B, 드롭 0, verify PASS·render clean. 사용자 확인: 전량(무필터, 논문 소거 실험 근거)·math 어댑터 포함·비율 설계 없음·보존 렌더. 자체 합성 트랙은 폐기(`alpha-SFT-Terminal-v1`·bins 삭제, 원본 9.8 GB 보존, `sdg/terminal/SYNTHESIS_ARCHIVE.md`); 그 과정의 phase-3 본 런(09-09 21:07 자동 개시 → iter 30 종료, 사용자 결정·phase-2 회귀 교정 우선)도 아카이브에. 유지된 수정: surplus 배관·sub1 jit595 스모크·필터 보정. 정본 `sdg/terminal/README.md` | ① B안 블렌드 확정 시 6 멤버 등록(크기 비례 가중, ep≤1, `mid-level-dataset-surplus 0.05`, 생성·스모크 담당 가능) ② TB-2 before/after 는 B안 phase-2 완주 후 ③ 영구 compat 수정(사용자, root) — jit595 우회로 sub1 학습은 가능 | `sdg/terminal/README.md`, `KNOWN_ISSUES.md` 09-10, `SFT_RL_DATASETS.md` §2.9 정정, arXiv 2602.21193 |
| **SFT 준비** | 64k 21종 + 128k 6종 변환·`verify_sft_bins` 전 PASS. `sft_40b_blend.yaml`(SWE 1-pass 앵커 40B) + `sft_128k_blend.yaml`(5.54B). preset `sft_64k.yaml`(CP4+offload, DP2) / `sft_128k.yaml`(CP8). interleaved-thinking 규약 정비 완결(a2f8894), agentic_v2 미편입 확정. **effort/budget NVIDIA 레시피 재현 확정(사용자, 08-25)**: 변환기 `--medium-effort`·`--truncate-reasoning-budget` 구현, 테스트 34/34·34/34, 스모크 bins 게이트 PASS (`SFT_RL_DATASETS.md` §2.6) | ① LongBlocks 변환기(1.5% 슬롯) ② RULER ③ 본 런 — LC 완료 후 ckpt 경로만 채움. ④ ~~effort 재변환~~ 완료(08-25): 3종 변환·게이트 PASS·블렌드 반영(chat 21→20+budget 1, ep 2.67/1.19). **GBS 192/96 확정(사용자, 2026-08-28)** — 12.58M tok/iter 전 스테이지 상수 관례 복원(초기 GBS 64는 Ultra 샘플 수만 미러한 오류; Ultra SFT 실제 18.9M). ETA 불변: **64k 3,179 iters ≈ 9.7일 + 128k 441 iters ≈ 1.7일**. `sft_64k.yaml` load = LC-B iter320 정본 기입 완료. **단일 128k 혼합 버킷으로 확정(사용자, 2026-08-28)** — 투버킷(64k 40B + 128k 꼬리) 폐기. 실측: 128k·CP8+offload·GBS 96 실블렌드 200.1s/iter = 62.9k tok/s·260 TFLOP/s(64k·CP4 GBS64 실측 47.5k 대비 1.32× — 12.58M 배치에서 iter 고정비 희석), 혼합 패킹 fill 98.7~100%, max-alloc 55~59GB. 본 런 = `sft_128k_full.yaml` × `sft_128k_mixed_blend.yaml`(26멤버, 51.34B = **2,448 iters @ GBS 160 = 20.97M tok/iter**(사용자, Ultra 18.9M 스케일), SWE 9.61B 정확 1ep, 전 꼬리 epoch 비례 편입) ≈ **9.3일**(실측 329.7s/iter·63.6k tok/s; GBS 96 대비 +1% — 처리량 동일, 근거는 최적화 설계). 64k·CP4·GBS192 비교 실측 65.4k tok/s = CP8 과 동률(1.53× 는 GBS64 착시). LR = Ultra 비율 이식 2.5e-5→1.5e-6 cosine(warmup 115). load = LC-B iter320. **본 런 개시 2026-08-28 08:19 KST** (`outputs/alpha_baseline_48L_sft_128k_full_20260828_081911`, wandb online, save/eval 300 iters = 8회+종료). KoChat 2차 트랜치 반영 검증 완료(v2 MANIFEST cut=tranche2 누적, `_t2` 3종만 편입). ETA 2,448 iters × 329.7s ≈ 9.3일 → **~09-06**. 조기중단 가드: valid loss 300 iters 마다. 구 sft_64k/sft_128k 프리셋은 superseded 표기 보존. **1노드 학습 확정(사용자, 2026-08-25)** — 2-node 단축안 기각. 유휴 노드는 SFT 후 RL 환경 검증용(별도 워크스페이스, 아래 보류 표) | `INTERLEAVED_THINKING.md`, `SFT_RL_DATASETS.md`, `DATA_PREP_LOG.md` |

| **SFT 벤치마크 (sub1)** | **phase-1 계열 iter300~1800 측정 완료, 최종 iter2448 기준선 미측정.** 수치 `results/TRACKING.md`, 체크포인트별 범위·중단 기록 `SFT_BENCHMARKS.md` §3.10. T1·T3 는 iter300~1800 전 구간, T2·에이전틱은 iter1500 까지(1800 은 사용자 결정으로 SWE 까지). **iter2448**: 09-14 11:07 KST 다른 세션이 T1·T3 기동 → T1 94.3% 에서 sub1 이 τ³ 스모크로 재배정되며 **결과 없이 중단**(프로브만: identity FAIL · 유령 호출 1/33 PASS). **평가 규약 변경 (09-14)**: 에이전틱 fleet 가 reasoning 파서 없이 떠 **모든 응답의 `</think>` 가 소실**돼 있었다(SWE 보존 0/148,836턴, `KNOWN_ISSUES` 09-14) → fleet `nemotron_v3` + 게이트 A5, SWE·TB-2 는 컨테이너 내 `tau_proxy` 로 이전 턴 추론 복원(SWE :8110 · TB-2 :8111). 사용자 규칙: **도구 사용 평가 = restore, 무도구 = strip**(학습 데이터와 동일). 스모크 검증 restore·strip 양쪽 PASS(d8d40bf). Terminal 정본은 TB-2(Harbor + Terminus-2, 09-07). SWE 인스턴스 이미지는 실행 후 정리(09-08 표준 정책). τ³ 트랙은 §3.13(integrate-tau3-bench-alpha 세션). **관찰(n=1)**: TB-2 restore 에서 `</think>` 미종결 턴이 이력에 되돌아가 루프로 굳음(restore 385/425 vs strip 38/262 스텝, NTC 미학습 ckpt) → 러너가 `steps_reasoning_only` 기록 | ① **iter2448 전 티어 실행 중 — sub1 점유 (09-14 12:51 UTC · 21:51 KST 기동, 약 1.5일, 사용자 결정. 21:22 1차는 sub1 HOME 볼륨 ENOSPC 로 fleet 실패 → 캐시 이전 후 재기동. **T1·T3 완료(09-15 03:39 KST, 전 항목 유효·AIME/HMMT 제외)**, 에이전틱 fleet 가 옮긴 캐시 절대경로로 사망 → 캐시 삭제 후 에이전틱·T2 만 07:36 KST 재기동(로그 `iter2448_suite_agentic_t2.log`))**: `eval_new_ckpt.sh … 2448 t1,t3,agentic,t2`, 로그 `bench_logs/iter2448_suite.log`. T1(~5.5h) → T3 → 에이전틱(SWE·TB-2·τ³) → T2. **도중 sub1 재배정·eval_sft 셸 스크립트 제자리 수정 금지**(bash 가 실행 중 파일을 이어 읽는다) ② 최종 SFT ckpt 에서 TB-2 미종결 지표 확인 ③ 아래 열린 결정 | `SFT_BENCHMARKS.md` §0·§3.10·§3.14·§7, `results/TRACKING.md` |
| **NeMo-Gym 채택 (SFT 단계, 사용자 결정 2026-09-15: v0.6.0 고정·환경은 우리 리포·범위 = RL 재사용 층)** | **1단계 CPU 스모크 완료 09-15** — Gym v0.6.0 을 `/home/work/vidsearch/tools/Gym`(NFS, Python 3.13.14 uv venv) 에 설치. 단위 테스트 main1 3,934/3 실패(스폰 테스트, sub1 재실행 3/3 PASS·GPU 장애 영향)·E2B 1건 선택 의존성. sub1 CPU 롤아웃 `mcqa`×`simple_agent`×Gemini 3/3 reward 1.0. **main1 은 GPU 장애 중 Ray 기동 즉사**(libcudahook). τ² 데이터 278행 준비, 우리 설정 4종(`examples/alpha/gym/configs/`) `gym env validate` PASS, 추론 왕복 판정기 합성 자기검증 PASS. 분석 정본 `study/nemo_gym_assessment.md` | ① **2단계 추론 왕복 게이트** — fleet(TOOLS=1·nemotron_v3) 여유 시 `gym/smoke/run_tau2_roundtrip.sh`(restore) + `strip_2turn.jsonl`(strip), 09-14 스모크와 같은 기준 ② 통과 시 에이전틱 티어 이관 계획 보고(iter2448 구 하니스 수치로 등가성 대조) → 승인 후 진행 ③ 유령 호출 프로브 리소스 서버 | `examples/alpha/gym/README.md`, `study/nemo_gym_assessment.md` |

| **채팅 서빙 (main1 GPU3)** — ⚠ **다른 컴퓨팅 세션의 노드**(같은 워크스페이스 공유로 혼입, 사용자 확인 2026-09-15; 이 클러스터 main1 아님) | **가동 중 — 2026-09-09 UI 를 OpenWebUI → LibreChat 으로 교체.** phase-2 iter500 을 vLLM :8001(128K 창, KV 1.31M 토큰) + LibreChat :8080(Node 20 + MongoDB 8.0, NFS `tools/librechat{,_data}`, docker 없음) 으로 서빙. 교체 사유: Open WebUI License 브랜딩 조항 + OpenWebUI 0.11 내장 도구 25종 주입으로 프롬프트 17→5,440 토큰·응답 이상 (`KNOWN_ISSUES` 09-09). 검증: `chat/smoke_chat.sh` vLLM 9/9 · UI 게이트 7/7 (프롬프트 증분 17 토큰 = 학습 렌더). G2 는 reasoning 파서 때문에 설계상 FAIL(`chat/README.md` §3) | 새 ckpt 나오면 `chat/serve_chat.sh <ckpt>` 로 교체. LibreChat 버전 올릴 때는 다른 포트로 먼저 띄워 §6 통과 후 이관 | `examples/alpha/chat/README.md` |
| **512K 추론 확장 (sub1)** | **착수 2026-09-01.** 추론-only YaRN 프로파일로 512K 창 판정 (학습 없음; 네이티브 256K+ 학습은 메모리 불가). 인프라 완료: `tools/set_long_context_config.py`(config 프로파일, CPU 프로브 PASS) · RULER `_512k` 태스크 4종(구간 131K/258K/393K/520K, `_aa` 불변) · `scripts/lc512k_grid.sh`(sub1 suite 종료 대기 후 자동 실행, 6셀 ≈ 2h). **그리드 6/6 완주(09-03 02:41 KST)** — 대조군이 셀1 해석을 기각: ext 도 single_1 무절벽(SFT 는 무보정 520K 95~100). 실제 YaRN 효과는 어려운 검색에서 보간 강도 순 단조(single_2 520K 0→0→25, multikey 0→10→40; ext→yarn2→yarn4), yarn4 단문 무회귀. base 셀은 chat 하니스 교란으로 판독 제한. 정본 `study/lc_512k_eval.md` §5. 능력 스위트 완료(09-03 11:07): 11태스크 평균 yarn2 48.1/34.2/26.1/18.1 · yarn4 45.7/37.1/31.8/24.8 — **전 구간 <85, 병목은 위치가 아니라 능력**(131K 에서 이미 45~48, multikey_2/3 전멸). **종합 판정: YaRN 채택(yarn4, >256K 전용 분리 서빙 — 131K 집계형 비용 때문), '지원 길이' 선언은 SFT 완주 ckpt 재측정으로 연기** (`study/lc_512k_eval.md` §5.1). **판정 재정의(사용자 승인 09-02)**: single_1 은 메커니즘 탐침일 뿐 — "L 지원" 선언은 **RULER 11태스크(`ruler_cap_*`, n=50) 구간 평균 ≥85**(RULER 논문 규약)로만. 능력 스위트 구축 완료(qa_squad·cwe 는 풀 고갈 실측으로 제외 — 사전 빌드 게이트가 cwe ~130K 포화 검출), `scripts/ruler_cap_run.sh` 가 그리드 DONE 후 자동 실행 예약 | **SFT 완주 ckpt 재측정 대기** — ① `ruler_cap_run.sh sft:yarn4` + `sft:ext` 대조(~5h) ② 판정 사다리(§5.1)로 지원 길이 선언 ③ mk2/3·MRCR·LC-C 는 §5.1 미결 목록. 512K 트랙 GPU 작업 전부 종료(09-03 11:07) — **iter1200 스위트 개시 가능** | `study/lc_512k_eval.md` |

## 보류·재평가 대기

| 항목 | 판정 | 재평가 시점 | 정본 |
|---|---|---|---|
| FlashQLA (TileLang GDN 커널) | 정확도 ✅, 성능 **채택 보류** — LC-A 형상(CP4 head-split)에서 fla 1.6~1.9× 우세, 128K 형상만 qla 2.2× 우세 | LC-B(128K) 국면 | `study/flashqla_poc.md` |
| Muon offload chunk/fraction 튜닝 | 256MB 기본으로 GO. 미튜닝 | LC-B | `MUON_OFFLOAD_BACKPORT.md` |
| **NeMo-RL post-training (RLVR→teacher RL→MOPD)** | **이 리포 범위 밖 — 보류** (사용자, 2026-08-25). 별도 워크스페이스 `project_s/NeMo-RL/`(브랜치 `alpha/post-train`, 최신 83976fe 08-24)에서 게이트 8종 통과: mcore+vLLM GRPO 퀵스타트, AlphaBridge 라운드트립 14,181/14,181, forward 패리티 cos≥0.99988, alpha vLLM 플러그인(stock 0.25.1), refit_verifier(mult_prob_err≈1.02), RL 블렌드, FlashQLA 커널. 남은 것 = GRPO 레시피 yaml(+ `env.nemo_gym.effort_levels` 마커 길이보상 설정, `SFT_RL_DATASETS.md` §2.6) + 8-GPU KL 게이트 | SFT 완료 후, 유휴 노드에서 재개 | `project_s/NEMO_RL_SETUP.md`(운영), `project_s/ALPHA_POSTTRAIN_PROGRESS.md`(경과), `NeMo-RL/examples/configs/alpha/README.md` |
| moe-recompute 해제 레버 | LC-A 본 런에서 OOM(68.9GB)으로 원복(9b1e7b1). 스모크 A/B(+15.5%)가 프로덕션 블렌드 꼬리를 못 봄 | resume 시 MoE 토큰 통계와 함께 | KNOWN_ISSUES 없음 — `gdn_cp_port.md` |

## 완료 (2026-08)

| 트랙 | 결과 | 정본 |
|---|---|---|
| **SFT phase-1 (교체 재개 런 `…swap_20260901_101523`)** | **⛔ 폐기 2026-09-13 (사용자) — 최종 단일 SFT 로 대체, ckpt 는 보존.** **완주 2026-09-07 16:21 KST** iter 2,448 (324 s/iter). final train loss 0.678, valid 0.5734 / PPL 1.774 (iter 2400 0.5729), ckpt iter_0002448(optimizer 포함) = **phase-2 load 정본**. 계보: 08-28 본 런 0~900 → 09-01 iter 900 데이터 교체(opencode_fixed·identity_v2, 가중치 비트 동일) → 2448. 교체 경위·게이트는 §10, 사고는 `KNOWN_ISSUES` 09-01 ④⑤⑥ | `SFT_PHASE2_PLAN.md` §10, `SFT_RL_DATASETS.md` §2.7 |
| **ko_chat 한국어 chat 합성 v1/v2** (sub1, 2026-08-23~28) — ⛔ **REJECTED 2026-09-09** (비-reasoning 교사 gemma-4-31B 의 가짜 reasoning, `KNOWN_ISSUES` 09-09; 재합성은 위 ko_chat v3 행) | **종료 2026-08-28 (사용자 결정 — sub1 은 SFT 이후 RL 검증 자원으로 전환)**. 교사 gemma-4-31B(vLLM TP2×DP4, ~3.4k tok/s) + DataDesigner. 최종 = 2차 트랜치 `alpha-SFT-KoChat-v2/` → `sft_packed_64k_pad16/{kochat_if_fanout_me_t2, kochat_chat_t2, kochat_b_fanout_t2}`: **A 544,771행(IF 번역 164,560 fan-out·chat 재생성 380,211) + B 네이티브 33,831행(r1+r2 strict-facts) = 한국어 real 1.35B**, verify PASS, `sft_40b_blend.yaml` 반영(chat 슬롯 8.77B 불변, 한국어 chat = 영어 chat_v3 **동일 epoch 1.89**, 한국어 학습 2.55B = 슬롯 29.1%). **최종 yaml 스모크 PASS(08-28 13:22, sub1, LC-B iter320·GBS 192)**: 2-iter loss 1.167→1.147, max-alloc 55.2GB, 186 TFLOP/s/GPU, traceback 0 — 24종 캐시 프리빌드 완료, **sub1 GPU 8장 비움(vLLM 미재기동)**. 사고·교훈: reasoning 소실(폐기 후 재생성), OxAlpha 1,000/일 상한 → 심판 캘리브레이션 전용 → 08-27 퇴출(B 미검증), B r1 DD 정체(타임스탬프 dir 직접 resume), 특수토큰 4중 가드. 미소비: A 시드 ~200k·B r2 85k·리젝 16.6k(재개 절차는 README). 20개 언어 chat 보강 미착수 | `examples/alpha/sdg/ko_chat/README.md` |
| Pre-training stage2 (DiLoCo 2노드) | **완주 2026-08-22** iter 26,832, train 1.145/1.141, valid(P3) 1.1658. 커리큘럼 자연→P2→P2b→P3 + bias-sync + 블록-순환 샤딩 적용. **stage3 없음(사용자 확정)**. 1-노드 재저장본 `outputs/alpha_baseline_48L_stage2_20260822_123916/checkpoints`. 완주 벤치마크 평가 결과는 미기록 — 실행했다면 여기 갱신 | `STAGE2_CURRICULUM_LOG.md` |
| **LC-B 128K@CP8 CPT** | **완주 2026-08-27** 320 iters (4.03B tokens, GBS 96·LR 7.5e-6 constant), final valid 1.6744/PPL 5.34. **최종 ckpt = `outputs/alpha_baseline_48L_lc_b_resume_20260826_223836/checkpoints` iter 320** (optim 포함 153GB) — **SFT load는 반드시 이 경로** (run1 033011은 iter150에서 검증 중단, resume1 172926은 iter222 크래시 — 둘 다 최종본 아님). 중간 검증 5종 PASS: NIAH 200/200·위치별 NLL 위치-단조(+0.089@120k)·표준벤치 Δ−0.10pp(망각 無)·CP8 backward 등가·GBS 노이즈 분석 (`outputs/lc_b_midrun_eval/`). 사고 2건: 자동연계 NaN 게이트 오탐(수정 bb279c9)·재개1 rank6 cuDNN driver 실행실패(일과성 판정, 재발 시 GPU6 하드웨어 의심). **최종 평가 완료(08-27)**: 벤치 11종 Δ−0.26pp(망각 無)·NIAH 4k~131k 200/200·**256K 95%(LC-A 42.5% 대비 — LC-B가 만든 외삽력)**·384K 0%(실사용 창 ~256K)·512K는 fla 커널 층당 작업공간 한계로 측정 불가 — 정본 `study/lc_b_final_eval.md`. RULER 풀 하니스(13태스크)는 미구축 | `configs/training/lc_b.yaml`·`lc_b_resume.yaml` 헤더, `LC_DATASETS.md` |
| **LC-A 32K@CP4 THD CPT** | **완주 2026-08-26** iter 1113 (14B tokens), final valid **1.4420 / PPL 4.23** (iter100 1.5357 대비 −0.094). iter100 조기 검증 GO(NLL 위치-단조, NIAH 160/160), 사고 1건(iter170 리터럴 EOD) 수리 포함. 최종 ckpt `outputs/alpha_baseline_48L_lc_a_resume_20260823_070651/checkpoints`(weights-only). 롤링 검증(`outputs/lc_a_early_eval/run_evals.sh`)은 미실행 — 4-GPU 창구 필요, 사용자 판단 | `configs/training/lc_a.yaml`, `study/lc_a_early_eval.md`, KNOWN_ISSUES 08-23 |
| LC 진입 게이트 | 판정 1~6 통과, LC-A GO. qk-clip은 LC preset에서 제거(TE 2.9 thd 비호환, max logit 19.4 = 임계 1/5) | `LC_ENTRY_GATE.md` |
| GDN CP + varlen/THD 스티치 | main 흡수(c9f65ad). CP{1,2,4} 32K 실데이터 등가 1.2e-4. 잠복버그 4건 수정 | `gdn_cp_port.md` |
| Muon chunked offload 백포트 | S0~S5 완료, main 흡수. 128K@CP8 72.5GB(OOM) → 54.9~58.8GB GO | `MUON_OFFLOAD_BACKPORT.md` |
| Nemotron specialized 데이터 | P2/P2b/P3 커리큘럼으로 투입 완료 (stage2 완주에 포함). 라이선스 주의: Multiple-Choice/Generative subset은 DeepSeek-v3 산출물(의무 조항), Wiki-Rewrite/Scientific-Coding은 CC BY-SA/GFDL; CC-Code-v1은 NVIDIA Open Data | `STAGE2_CURRICULUM_LOG.md` |
| Claude Code 설정 영속화 | `~/.claude`가 휘발 경로였음 → `CLAUDE_CONFIG_DIR=/home/work/vidsearch/.claude-config/<노드>` (2026-08-25). 재시작 후 `setup_pai_megatron_env*.sh`가 자동 source | `/home/work/vidsearch/setup-claude.sh` |

## 열린 사용자 결정

| 결정 | 선택지 · 권고 | 정본 |
|---|---|---|
| 09-14 이전 에이전틱 수치(iter300~1800 SWE·TB-1) 표기 | 구 조건 계열로 유지(계열 내 추이만 유효) vs 무효 처리 | `SFT_BENCHMARKS.md` §3.10 · `KNOWN_ISSUES` 09-14 |
| TB-2 restore 에서 `</think>` 미종결 턴 복원 제외(`tau_proxy` 옵트인 플래그, τ³ 세션 제안) | **권고: 보류** — NTC 학습 ckpt 에서 `steps_reasoning_only`·`think_unclosed_stop` 이 0 에 가까우면 불필요. 남으면 플래그 스모크 후 결정 | `SFT_BENCHMARKS.md` §3.14 |
